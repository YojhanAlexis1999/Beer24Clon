import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    botonesInicio: {
        position: 'absolute',
        bottom: 80,
        left: 41,
        flexDirection: 'row',
        justifyContent: 'space-around'
    }
})
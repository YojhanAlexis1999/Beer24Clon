import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    titulo: {
        color: '#fff',
        fontSize: 20,
        textAlign: 'center',
        marginTop: 60
    },
    button: {
        width: '90%',
        height: 50,
        borderRadius: 30,
        borderColor: '#000',
        borderWidth: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
    },
    buttonText: {
        color: '#000',
        fontSize: 30
    }
})